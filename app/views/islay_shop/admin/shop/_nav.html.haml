- sub_nav('Overview', path(:catalogue), :root => true, :class => 'overview')
- sub_nav('Categories', path(:product_categories), :class => 'folder')
- sub_nav('Ranges', path(:product_ranges), :class => 'folder')
- sub_nav('Products', path(:products), :class => 'basket')

- control('Category', path(:new, :product_category), :new)
- control('Range', path(:new, :product_range), :new)
- control('Product', path(:new, :product), :new)
