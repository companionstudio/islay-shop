%ol.order-progression.inset
  - process.each do |step|
    %li{:class => "#{step[:action]} #{step[:done] ? 'done' : ''} #{step[:current] ? 'current' : ''} #{step[:error] ? 'error' : ''}"}
      %span.order-progression-el-label= step[:label]
      %span.order-progression-el-indicator
