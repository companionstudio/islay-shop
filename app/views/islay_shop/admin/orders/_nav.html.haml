- sub_nav('Orders', path(:orders), :root => true, :class => 'overview')
- sub_nav('Process', path(:order_processes), :class => 'basket')
- sub_nav('Completed', path(:order_archives), :class => 'folder')
