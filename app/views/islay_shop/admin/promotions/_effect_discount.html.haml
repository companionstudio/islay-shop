= f.fieldset("A Discount on Their Order", effect_opts(effect)) do
  = f.hidden_field(:type)
  .effect-toggle
    = f.input(:active, :as => :boolean, :first_inline => true)
    
  .effect-contents
    %p.summary
      Apply a discount to the order total

    = f.input(:amount, :class => 'tiny', :label => "Amount", :inline => true)
    = f.input(:mode, :collection => %w(dollar percentage), :as => :radio_buttons, :inline => true)

    .help
      .extra.islay-toggle
        %p Discounts can be either a fixed dollar amount or a percentage of the order total.

        %p The discount is applied to the order's grand total, including products, shipping and other services.

